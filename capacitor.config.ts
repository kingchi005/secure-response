import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.secureresponseconnect.app",
	appName: "secure-response-connect",
	webDir: "dist",
};

export default config;
